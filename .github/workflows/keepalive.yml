name: Keepalive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Ping Render health check
        run: curl -sf "${{ secrets.RENDER_URL }}/api/health" || echo "Backend unreachable"
